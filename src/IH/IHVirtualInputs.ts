/**
 * This is a helper enum where you can assign strings to an enum in case you don't want to check against strings directly.
 * For example: IH.AddVirtualInput('up'); becomes IH.AddVirtualInput(IHVI.up);
 */
